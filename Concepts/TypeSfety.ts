//JS is dynamically typed programming language

let newage=30;
console.log(typeof(age));

//TS IS STATICALLY TYPED PROGRAMMING LANGUAGE
let newage1:number=50;
console.log(typeof(newage1));

//so it has typesafety>>JS not typesafe
//Annotation:number
//Type reference
let a=20;